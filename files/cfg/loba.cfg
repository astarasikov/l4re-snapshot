-- vim: set ft=lua:

local loba_caps = {
  lb = L4.default_loader:new_channel();
};

L4.default_loader:start(
  { caps = {lb = loba_caps.lb:svr(), }, log = { "loba", "cyan" } },
  "rom/loba");

L4.default_loader:start(
  { scheduler = loba_caps.lb:create(L4.Proto.Scheduler, 
                                    "client name=hello prio=1"),
    log = { "hello", "Yellow" },
    l4re_dbg = L4.Dbg.Warn
  },
  "rom/hello");
